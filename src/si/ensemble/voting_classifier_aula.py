

class VotingClassifier:

    def __init__(self, models):
        self.models = models

    def fit(self):
        for model in self.models:
            model(fit)


    def predict(self):
        #o que o ensemble faz

        #Obtém previsões de cada modelo

    def get_majority_vote():
        labels, counts = np.unique(pred, return_counts = True)
        return labels(np.argmax(counts))

        predictions = np.array(model.predict)


    def score(self):
        #no exemplo o modelo final é o KNN classifier







